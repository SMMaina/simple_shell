#include <stdio.h>

/**
 * main - prints an integer
 * Return: 0 if successful
 */

int main(void)
{
	int n;

	printf("Enter an integer: ");
	scanf("%d", &n);
	printf("You entered: %d", n);

	return (0);
}
